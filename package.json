{
  "name": "visibilityevents",
  "version": "3.0.0",
  "description": "This library defines an object with two functions to detect visibility (total or partial) of an element in the viewport, and two functions that simulate two nonexistent events to detect when any element enters or leaves the visible area of the page.",
  "main": "visibilityEvents.js",
  "scripts": {
    "minimize": "uglifyjs visibilityEvents.js --compress --mangle --comments --source-map filename=visibilityEvents.min.js.map -o visibilityEvents.min.js "
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jmalarcon/visibilityEvents.js.git"
  },
  "keywords": [
    "js",
    "javascript",
    "visibility"
  ],
  "author": "José M. Alarcón",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/jmalarcon/visibilityEvents.js/issues"
  },
  "homepage": "https://github.com/jmalarcon/visibilityEvents.js#readme",
  "devDependencies": {
    "uglify-js": "^3.5.2"
  }
}
