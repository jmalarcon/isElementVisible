{
  "name": "iselementvisible",
  "version": "2.0.0",
  "description": "This library defines an object with two functions to detect visibility (total or partial) of an element in the viewport, and two functions that simulate two nonexistent events to detect when any element enters or leaves the visible area of the page.",
  "main": "isElementVisible.js",
  "scripts": {
    "minimize": "uglifyjs isElementVisible.js --compress --mangle --comments --source-map filename=isElementVisible.min.js.map -o isElementVisible.min.js "
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jmalarcon/isElementVisible.git"
  },
  "keywords": [
    "js",
    "javascript",
    "visibility"
  ],
  "author": "José M. Alarcón",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/jmalarcon/isElementVisible/issues"
  },
  "homepage": "https://github.com/jmalarcon/isElementVisible#readme",
  "devDependencies": {
    "uglify-js": "^3.4.7"
  }
}
